{
  "name": "webhook-receiver",
  "version": "1.0.0",
  "description": "Cloudflare Worker — GitHub Projects v2 webhook → gsd-saas-creator dispatch",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241022.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}
