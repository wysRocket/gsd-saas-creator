# STITCH Prompt — GuidEnza

## Product Context
GuidEnza is an AI-powered platform designed to help educators and institutions create, manage, and deliver personalized learning pathways and adaptive curriculum content. It streamlines the process of tailoring educational experiences to individual student needs, improving engagement, and optimizing learning outcomes through intelligent content generation and progress tracking.

## Design Style
Style: Modern Tech with a Premium feel, leaning towards a sophisticated dark mode aesthetic.
Mood: Trusted, Innovative, Empowering, Intelligent.
Inspiration: neuform.ai

## Color Palette
Primary: #fff0c5
Background: #050505
Surface: #1c1c22
Text: #ffffff (primary), #c8ccd4 (muted)
Accent: #7684ff (interactive elements), #9db1ff (subtle emphasis)

## Typography
Heading font: Inter, 300-500
Body font: Segoe UI, 400

## Page to Generate: Landing Page

### Layout
The page consists of vertically stacked, full-width sections. Content within each section is centered and constrained to a maximum width of 1280px, using a 12-column grid system with a base spacing unit of 2px. The background is predominantly dark (`#050505`) with subtle radial gradients (`radial-gradient(circle, rgba(255, 255, 255, 0.02), rgba(0, 0, 0, 0) 58%)`) for depth.

### Hero Section
Headline: "Your AI Guide to Personalized Education."
Subheadline: "GuidEnza streamlines curriculum creation and student progress tracking, empowering educators to deliver adaptive learning experiences with intelligent content generation."
CTA: "Start Personalizing Education" (Primary button, `#fff0c5` background, `#050505` text, full border radius, subtle box shadow). "Learn More" (Secondary button, transparent background, `#7684ff` border and text, full border radius, subtle box shadow).
Visual: Dark background (`#050505`) with subtle radial gradients and a minimalist, abstract AI-themed illustration or subtle glowing particles to convey innovation and intelligence.

### Features Section
Layout: A grid of 3-4 cards per row on desktop, centered within the max-width container. Each card has an icon, a bold title, and a brief description.
Content:
- Card 1: AI-assisted curriculum generation. Icon: A stylized brain or lightbulb. Description: "Effortlessly create adaptive lesson plans tailored to individual student needs."
- Card 2: Centralized content library. Icon: A book or folder. Description: "Organize, store, and reuse all your educational materials in one secure place."
- Card 3: Learner progress tracking. Icon: A chart or progress bar. Description: "Monitor student performance and identify areas for improvement with intuitive analytics."
- Card 4: User management. Icon: A group of people or gears. Description: "Easily manage classes, groups, and educator access within your institution."
Visual: Cards have a surface background (`#1c1c22`), medium border radius (`6px`), and a subtle `lg` inset box shadow. Icons are in `#7684ff`. Text is `#ffffff` for titles, `#c8ccd4` for descriptions.

### Pricing Section
Headline: "Flexible Plans for Every Educator and Institution"
Layout: Three distinct pricing cards arranged horizontally on desktop, centered within the max-width container. Each card clearly outlines the plan name, price, key features, and a call to action.
Content:
- Plan A: Educator Pro: $49/mo. Features: 1 educator seat, unlimited classes, 50 AI curriculum generations/month, basic learner progress tracking, core content library access. CTA: "Choose Plan" (Primary button).
- Plan B: Department Plus: $199/mo. Features: Up to 10 educator seats, unlimited AI curriculum generations, advanced analytics & reporting, priority support. CTA: "Choose Plan" (Primary button).
- Plan C: Institution Elite: $499/mo. Features: Unlimited educator seats, full AI capabilities, custom branding, API access, dedicated account manager, enterprise-grade security. CTA: "Choose Plan" (Primary button).
Visual: Pricing cards use the surface background (`#1c1c22`), medium border radius (`6px`), and a subtle `lg` inset box shadow. The active or recommended plan might have a subtle border in `#7684ff`. Plan names are `#ffffff`, prices are large and in `#fff0c5`, features are `#c8ccd4`.

### Footer
Layout: Full-width, dark background (`#050505`). Contains navigation links, brand information, and copyright.
Content:
- Links: About Us, Features, Pricing, Contact, Privacy Policy, Terms of Service.
- Brand: GuidEnza © 2024
Visual: Text links are `#c8ccd4` and change to `#ffffff` on hover. Brand name is `#fff0c5`.

## Page to Generate: Educator Dashboard

### Layout
The page features a sticky top navigation bar. The main content area is divided into several sections: overview cards, a list of classes, a student progress snapshot, and quick actions. Content is centered within a 1280px max-width container, using a 12-column grid.

### Top Navigation
Layout: Sticky to the top of the viewport. Brand logo on the left, main navigation links (Dashboard, Curriculum, Students, Library) centered or right-aligned, and user profile/settings on the far right.
Content: "GuidEnza" logo (text in `#fff0c5`), Navigation links (Dashboard, Curriculum, Students, Library), User profile avatar (circular, `999px` border radius) with user's name or icon.
Visual: Dark background (`#050505`), subtle bottom border (`1px solid rgba(255,255,255,0.1)`). Navigation links are `#c8ccd4` and turn `#ffffff` with a subtle underline in `#7684ff` on hover/active.

### Overview Cards
Layout: A grid of 4 cards, evenly spaced, across the top of the main content area.
Content:
- Card 1: "Active Classes" with a numerical value (e.g., "12").
- Card 2: "Total Students" with a numerical value (e.g., "250").
- Card 3: "Curriculum Drafts" with a numerical value (e.g., "5").
- Card 4: "Recent Activity" with a brief summary (e.g., "3 new assignments graded").
Visual: Cards use the surface background (`#1c1c22`), medium border radius (`6px`), and a subtle `lg` inset box shadow. Titles are `#c8ccd4`, numerical values are large and in `#fff0c5`.

### My Classes
Headline: "My Classes"
Layout: A flexible grid or list of class cards below the overview section. Each card displays class information and quick actions.
Content: Each class card includes: Class Name (e.g., "Algebra I - 2024"), Number of Students (e.g., "30 Students"), Quick Actions (Buttons: "View," "Edit").
Visual: Class cards use the surface background (`#1c1c22`), medium border radius (`6px`), and a subtle `lg` inset box shadow. Class names are `#ffffff`, student count is `#c8ccd4`. Action buttons are small, transparent with `#7684ff` text, full border radius.

### Student Progress Snapshot
Headline: "Student Progress Snapshot"
Layout: A section containing a small chart or summary of overall student performance.
Content: A simplified bar chart or line graph showing average scores or completion rates across classes. A summary text (e.g., "Overall student performance is trending positively.").
Visual: Surface background (`#1c1c22`), medium border radius (`6px`), subtle `lg` inset box shadow. Chart elements use `#7684ff` and `#9db1ff`. Text is `#c8ccd4`.

### Quick Actions
Headline: "Quick Actions"
Layout: A panel or section with prominent buttons for common tasks.
Content: Buttons: "Create New Curriculum" (Primary button), "Add Student" (Secondary button), "Upload Content" (Secondary button).
Visual: Buttons use full border radius (`999px`) and subtle box shadows. Primary button has `#fff0c5` background, secondary buttons have `#7684ff` background.

## Page to Generate: Curriculum Builder

### Layout
The page is structured with a left-hand sidebar for navigation and a main content area on the right. The main content area is further divided into input fields, content blocks, and a preview panel. Content is within a 1280px max-width container.

### Sidebar Navigation
Layout: Fixed-width sidebar on the left.
Content: Navigation links for curriculum sections: "Objectives," "Content," "Activities," "Assessment."
Visual: Dark surface background (`#1c1c22`), medium border radius (`6px`) on the outer edge. Links are `#c8ccd4` and turn `#ffffff` with a subtle left border in `#7684ff` on hover/active.

### Main Content Area
Layout: The central part of the page, containing input forms and generated content.
Content:
- Input fields: Text areas and dropdowns for "Learning Objectives," "Student Profiles (e.g., Grade Level, Learning Styles)," and "Curriculum Parameters (e.g., Duration, Subject)."
- AI generation button: "Generate Curriculum" (Primary button).
- Content blocks/cards: Dynamically generated sections for lessons, activities, and resources. Each block has a title, description, and icons for drag-and-drop functionality, editing, and deletion.
- Rich text editor: For manual content editing within content blocks.
Visual: Input fields have a dark background, subtle border, medium border radius (`6px`). Focus state shows a thin border in `#7684ff`. Text is `#ffffff`. The "Generate Curriculum" button has a `#fff0c5` background, `#050505` text, full border radius (`999px`), and a subtle box shadow. Content blocks use the surface background (`#1c1c22`), `6px` border radius, and a subtle inset box shadow.

### Preview Panel
Layout: A right-aligned panel showing a live preview of the generated curriculum structure.
Content: A hierarchical outline view with collapsible sections for Units, Lessons, and Activities. Each item shows a title and a status indicator.
Visual: Panel background is `#141414`, bordered left by `1px solid rgba(255,255,255,0.1)`. Section titles are `#ffffff`, sub-items are `#c8ccd4`. Active/selected item has a left border accent in `#7684ff` and a background of `#252b39`.

## Page to Generate: Learner Profile & Progress View

### Layout
The page is organized into a profile header at the top followed by a two-column layout: a left column for detailed progress metrics and a right column for a learning pathway visualization. Content is centered within a 1280px max-width container.

### Profile Header
Content: Student name, avatar (circular, `999px` border radius), grade level, enrolled classes count, and overall progress percentage.
Visual: Dark background (`#050505`). Name is large `#ffffff` text. Progress percentage is displayed in `#fff0c5`. Avatar border uses `#7684ff`.

### Progress Metrics
Headline: "Performance Overview"
Layout: A grid of metric cards showing key stats, followed by a detailed breakdown table.
Content:
- Metric cards: "Average Score" (e.g., "84%"), "Assignments Completed" (e.g., "42 / 50"), "Time on Platform" (e.g., "12h this week"), "Streak" (e.g., "7 days").
- Breakdown table: Columns — Subject, Latest Score, Trend (arrow icon), Status. Rows per enrolled class.
Visual: Metric cards use surface background (`#1c1c22`), `6px` border radius, subtle inset box shadow. Metric values are large `#fff0c5`. Trend arrows are `#34D399` (up) or `#F87171` (down). Table rows alternate between `#141414` and `#1c1c22`.

### Learning Pathway Visualization
Headline: "Learning Pathway"
Layout: A horizontal or vertical node-based timeline showing curriculum milestones and completion state.
Content: Nodes represent lessons or units. Each node shows a title and a completion indicator (checkmark, in-progress ring, or locked icon).
Visual: Completed nodes use `#7684ff` fill with a checkmark in `#ffffff`. In-progress nodes use `#252b39` with a `#7684ff` border and a progress ring. Locked nodes are `#1c1c22` with a muted lock icon in `#c8ccd4`. Connecting lines are `1px solid #252b39`.

## Page to Generate: Content Library Management

### Layout
Full-width page with a persistent top navigation and a content area split into a filter sidebar on the left and a responsive grid of resource cards on the right. Max-width 1280px.

### Filter Sidebar
Layout: Fixed-width left column with collapsible filter groups.
Content: Filters — Resource Type (Video, Document, Quiz, Link), Subject, Grade Level, Date Added. A search input at the top. "Clear Filters" text link at the bottom.
Visual: Sidebar background `#141414`, `6px` border radius on the container. Filter group labels are `#c8ccd4`. Active filter chips have a `#7684ff` background and `#ffffff` text. Search input matches the standard input style with `#7684ff` focus border.

### Resource Grid
Headline: "Your Content Library"
Layout: Responsive grid — 4 columns on desktop, 2 on tablet. Each card represents one uploaded resource.
Content: Each card shows — Thumbnail or file-type icon, Resource Title, Type badge (e.g., "PDF", "Video"), Subject tag, Date Added, and action icons (Preview, Edit, Delete).
Visual: Cards use surface background (`#1c1c22`), `6px` border radius, subtle inset `lg` box shadow. Thumbnail area is `#0d0d0d`. Title is `#ffffff`, metadata is `#c8ccd4`. Type badges are small pills — `#252b39` background, `#9db1ff` text. Action icons are `#c8ccd4` and highlight to `#ffffff` on hover.

### Upload Controls
Layout: A sticky bar at the bottom of the content area or a prominent button group above the grid.
Content: "Upload Resource" (Primary button), "Import from URL" (Secondary button), "Bulk Upload" (text link).
Visual: Primary button — `#fff0c5` background, `#050505` text, `999px` border radius. Secondary button — `#7684ff` background, `#ffffff` text, `999px` border radius. Text link — `#7684ff`.

## Component Specs

- **Buttons — Primary:** `#fff0c5` background, `#050505` text, `999px` border radius, `box-shadow: 0 0 20px rgba(255,240,197,0.15)`. Hover: slight brightness increase.
- **Buttons — Secondary:** `transparent` background, `#7684ff` border (`1.5px solid`) and text, `999px` border radius. Hover: `#7684ff` background, `#ffffff` text.
- **Cards:** `#1c1c22` background, `6px` border radius, `box-shadow: inset 0 1px 0 rgba(255,255,255,0.05)`.
- **Inputs:** `#252b39` background, `1px solid rgba(255,255,255,0.1)` border, `6px` border radius. Focus: `1px solid #7684ff`.
- **Nav (top):** Sticky, `#050505` background, `1px solid rgba(255,255,255,0.1)` bottom border. Links `#c8ccd4`, active/hover `#ffffff` with `#7684ff` underline.
- **Badges / Pills:** `#252b39` background, `#9db1ff` text, `999px` border radius, `12px` font size.

## Visual References

See screenshots in `design/` (designlang extraction from the competitor reference `neuform.ai`) and `design-language.md` for component-level visual tokens and spacing conventions. All token values in this prompt are sourced from `design.md` — treat it as the authoritative design contract.

## Output Requirements

- Full-width desktop layout (1280px max-content-width)
- Include hover states on all interactive elements (buttons, links, cards, nav items)
- Dark mode: yes (all surfaces dark; no light-mode variant needed for v1)
- Mobile responsive: yes — stack columns to single column below 768px; hamburger nav on mobile
