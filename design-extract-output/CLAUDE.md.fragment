## Design system (via designlang)

Source: https://kingdomofarcane.com
Extracted by designlang v7.0.0 on 2026-04-19T10:56:36.284Z

## Semantic tokens (use these)
- color.action.primary: #00d9ff
- color.surface.default: #0a0a0f
- color.text.body: #000000
- radius.control: 6px
- typography.body.fontFamily: Inter

## Regions
- nav

## How to use
- Prefer `semantic.*` tokens over `primitive.*`.
- Never invent new tokens or hex values; reuse the ones above.
- When a value is missing, pick the closest existing semantic token and flag the gap.
- Reference tokens by their dotted path (e.g. `semantic.color.action.primary`).
